<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesource.org/schema/mule/core/2.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:restlet="http://www.mulesource.org/schema/mule/restlet/2.0"
    xmlns:spring="http://www.springframework.org/schema/beans"
    xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
               http://www.mulesource.org/schema/mule/core/2.0 http://www.mulesource.org/schema/mule/core/2.0/mule.xsd
       http://www.mulesource.org/schema/mule/restlet/2.0 http://www.mulesource.org/schema/mule/restlet/2.0/mule-restlet.xsd">

    <model name="Test">

        <service name="logistics.1.buyer">
            <inbound>
                <inbound-endpoint address="restlet:http://localhost:9002/orders/orderId" synchronous="true">
                    <properties>
                        <spring:entry key="uriPattern"
                            value="/orders/{orderId}" />
                    </properties>
                </inbound-endpoint>
            </inbound>

            <outbound>
                <!-- send HTTP GET to the right service and only pull out the buyerId, not the content of the restlet request-->
                <outbound-pass-through-router>
                    <outbound-endpoint address="vm://test"
                        synchronous="true" remoteSync="true" />
                </outbound-pass-through-router>
            </outbound>

        </service>

        <service name="test">
            <inbound>
                <inbound-endpoint address="vm://test" />
            </inbound>
            <component class="org.mule.transport.restlet.DumpComponent" />
        </service>
    </model>
</mule>